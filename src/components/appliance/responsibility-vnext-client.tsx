"use client";

// Backward-compatible import path for any old references.
// The v2 Power Studio supersedes the original ACCESS / MEMORY / RUNTIME editor.
export { default } from "./responsibility-power-client";
